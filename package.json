{
  "name": "entrevistes-app",
  "version": "1.0.0",
  "description": "Entrevistes App - Sistema de gestión de entrevistas",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd client && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
